# 30 de abril
- Queremos fazer com erlang para comparar o que acontece quando trocamos a exponencial por erlang. Comparar com a curva do paper. Podemos escolher qual K-erlang. Queremos comparar méida e variança. Fazer modelos diferentes para exponencial, erlang e hyperexponencial

- Uma erlang por qbit, logo a quantidade de variáveis de estado é o número de qbits (0 se estiver vazio).

- O decaimento é PROBABILISTICO, logo não é porque um qbit esta numa fase mais adiantada que ele está a menos tempo no sistema, logo não podemos modelar "agrupado".

- Lembrar do sistema de duas filas como um todo: peer1 -> repeater -> peer2. Fila dupla

- Mu = 1/t*, lambda1 = chegada na fila 1; lambda2 = chegada na fila 2

- Qual o interesse na hyperexponential? hyperexponencial -> vai ser uma EXPn*P(n): um a priori

- Um bell pair herda o decaimento do qbit original 

- A bolinha parace (v1,v2) onde v1 e v2 pertencem a K (de K-erlang)